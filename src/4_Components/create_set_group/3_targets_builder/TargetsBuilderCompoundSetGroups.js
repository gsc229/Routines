import React, {useState} from 'react'
import { connect } from 'react-redux'
import ConnectedRowDynamicFieldInputDecIncLabeld from '../3_form_create_set_group/ConnectedRowDynamicFieldInputDecIncLabeld'




export const TargetsBuilder = (props) => {

  

  return (
    <div>
      
    </div>
  )
}

const mapStateToProps = (state) => ({
  
})

const mapDispatchToProps = {
  
}

export default connect(mapStateToProps, mapDispatchToProps)(TargetsBuilder)
